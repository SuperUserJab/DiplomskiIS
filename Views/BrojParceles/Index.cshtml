@model IEnumerable<DiplomskiISpoljoprivreda.BrojParcele>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.BrojParcele1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Opis)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Kvadratura)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.BrojParcele1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Opis)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Kvadratura)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ParcelaID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ParcelaID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ParcelaID })
        </td>
    </tr>
}

</table>
